services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: prowem:latest
    container_name: prowem
    ports:
      - "${PORT:-3000}:3000"
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
